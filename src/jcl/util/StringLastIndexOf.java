package jcl.util;



public class StringLastIndexOf {
    
    public static int last_index_of(String sa, String sb) {
        // V return sa.last_index(sb) or { -1 }
        return sa.lastIndexOf(sb); // JAVA ONLY
    }

}